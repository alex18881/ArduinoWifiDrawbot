//WIFI module
#include "Configuration.h";
#if defined(WIFI)
#include "ESP8266.h";
#include <SoftwareSerial.h>;

class Wifi
{
	public:
		Wifi();
		void init( );
		bool onConnected();
		uint32_t read(char *buff);
		void write(char *msg);
   
	private:
		bool inited = false;
		String status = "";
		uint8_t mux_id = 0;
		SoftwareSerial _serial = SoftwareSerial (WIFI_RX_PIN, WIFI_TX_PIN);
		ESP8266 _wifi = ESP8266 (_serial);
};
#endif
